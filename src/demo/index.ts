// The demo feature's public surface: DemoSurface only. Its geometry, gesture
// engine and panel UI are locality-scoped implementation details behind
// this one export (audit 2.1).
export { DemoSurface } from './DemoSurface';
